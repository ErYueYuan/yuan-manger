```javascript
  git push -u origin 分支名 -f  //强制提交
  git init 初始化
  git add .  添加全部
  git remote add origin git地址  建立远程库链接
  git commit -m "日志记录"  提交/记录
  git push -u origin 分支名称   推送到远程库
  git pull  分支名称    拉取远程库
  

```
